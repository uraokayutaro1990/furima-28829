function item() { 
 const price = document.getElementById("item-price");
 price.addEventListener("input", (e) => {
const formData = new FormData(document.getElementById("form")); 




 
}
window.addEventListener("load", item);

// 1000と入ったら、１０００を取得する。
// 取得した１０００をつかって、利益と手数料を計算する。
// 計算結果を表示する。